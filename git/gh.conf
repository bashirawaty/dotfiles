# ------------------------------------------------------------------------------
# GitHub CLI configuration
# Enables gh for PRs, issues, repos, and SSH authentication.
# ------------------------------------------------------------------------------

# Use SSH for GitHub operations
git_protocol = ssh

# Enable interactive prompts
prompt = enabled

# Default editor for gh commands
editor = vim

# Enable browser-based login
browser = true
