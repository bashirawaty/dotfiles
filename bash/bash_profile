# ------------------------------------------------------------------------------
# bash_profile — login shell initializer for macOS and Linux.
# Loads .bashrc so all interactive features work in login shells.
# ------------------------------------------------------------------------------

# Load .bashrc if it exists
if [ -f "$HOME/.bashrc" ]; then
    source "$HOME/.bashrc"
fi
